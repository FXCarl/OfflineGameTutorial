using UnityEngine;
using System.Collections;

public class DialogBoxManager : MonoBehaviour {

	// Use this for initialization
	void Start () {
        GetComponent<RectTransform>().anchoredPosition = new Vector2( 0f , -165f );
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
